[pytest]
testpaths = tests
python_files = test_*.py
# The agent package must be importable without installing it: on the ArcMap machine
# it runs from a checkout with ArcMap's Python 2.7, not from a virtualenv.
pythonpath = . ../tools/arcpy-double
